import React, {FC, ReactElement} from "react";

export const BetterGoalsLogo: FC = (): ReactElement => {

	return (
		<div style={{fontSize: "2em", textAlign: "right", width: "100%", userSelect: "none"}}>
			Better Goals 🎯
		</div>
	);
};

